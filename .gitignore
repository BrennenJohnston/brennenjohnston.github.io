# Working files from content extraction — contains unredacted source documents.
# Never commit this folder.
_extract/

# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

Gemfile.lock

# Gems installed into the repo by `bundle install --path vendor/bundle`, which
# is what ruby/setup-ruby does in CI.
vendor/
node_modules/
